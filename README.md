## 💾 Telegram RSS Embeder

**Вывод анонсов из Telegram-канала в статьи и шаблоны WordPress с гибкой настройкой.**

| Информация | Детали |
| :--- | :--- |
| **Авторы** | [Али Профи] |
| **Версия** | 1.1.0 |
| **Лицензия** | GPLv2 или более поздняя (Рекомендуется для WP) |
| **Совместимость** | WordPress 5.0+ |

### 🌟 Основные функции

Этот мини-плагин позволяет автоматически получать и красиво отображать последние записи (анонсы) из любого публичного Telegram-канала на вашем сайте WordPress, используя сторонний RSS-мост.

  * **Автоматическое обновление:** Анонсы обновляются автоматически при появлении новых постов в Telegram.
  * **Гибкое встраивание:** Поддержка шорткода (`[telegram_rss]`) для вставки в контент и прямой вызов PHP-функции (`display_telegram_announcements()`) для интеграции в файлы шаблона.
  * **Чистое оформление:** Выводит только заголовок поста, с миниатюрой слева (если доступна) и минималистичным стилем.
  * **Устранение ошибок форматирования:** Специальная обработка контента для предотвращения слипания слов при чтении RSS-ленты.

### 🚀 Установка

Поскольку этот код предназначен для встраивания в файл `functions.php` вашей темы, установка требует ручного копирования.

1.  **Откройте `functions.php`:** Перейдите в админ-панели WordPress в **Внешний вид** → **Редактор файлов тем**.
2.  **Выберите файл:** Найдите и откройте файл `functions.php` вашей активной дочерней темы (или основной, если вы не используете дочернюю).
3.  **Вставьте код:** Скопируйте весь предоставленный код (включая секцию `define` и все функции) и вставьте его в конец файла `functions.php`.
4.  **Настройка URL:** **Обязательно** замените URL фида в начале кода на ваш актуальный адрес RSS:
    ```php
    define('TELEGRAM_RSS_FEED_URL', 'ВАШ_АКТУАЛЬНЫЙ_АДРЕС_RSS'); 
    ```
5.  **Сохраните изменения.**

### 🖥️ Использование

После установки и настройки вы можете вывести блок с анонсами двумя способами:

#### 1\. Использование шорткода (для статей и страниц)

Вставьте этот шорткод в любой блок "HTML" или "Классический редактор":

```
[telegram_rss]
```

#### 2\. Вызов PHP-функции (для шаблонов)

Используйте этот код в файлах шаблона вашей темы (`single.php`, `sidebar.php` и т.д.):

```php
<?php echo display_telegram_announcements(); ?>
```

### 🤝 Вклад (Contributing)

Если у вас есть предложения по улучшению, обнаружены ошибки или вы хотите добавить новые функции (например, кастомные стили через настройки темы), пожалуйста, создавайте [Issue] или [Pull Request].
